import type { LoginOption } from '../types';

export const loginOptions: LoginOption[] = [
  { label: 'Login as Customer', href: '/login/customer' },
  { label: 'Login as Broker', href: '/login/broker' },
];
